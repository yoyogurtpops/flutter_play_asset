package example.android.editorassetpack

apply plugin: 'com.android.asset-pack'

assetPack {
    packName = "editorassetpack" // Directory name for the asset pack
    dynamicDelivery {
        deliveryType = "on-demand"
    }
}